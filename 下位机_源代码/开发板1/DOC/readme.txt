1、通用模板：Template可以用。在固件库中真正有用的只有Library里面的，将固件库中的Library拷到Template中的Library里，再建立新工程，将启动文件、内核寄存器映射C文件、系统时钟配置C文件、外设C文件等拷到Template相应的文件夹中。

2、在C/C++选项卡中添加处理宏及编译器编译的时候查找的头文件路径，如果头文件路径添加有误，则编译的时候会报错找不到头文件

3、REG_LED和PERIPH_LED这两个工程可以用，但这两个不是利用固件库编程，而是自己编写的固件库雏形，不完全。